[project]
name = "pattern-1-entra-direct-app"
version = "0.1.0"
description = "Pattern 1 sample: call Azure OpenAI with an Entra ID bearer token via the official openai SDK."
requires-python = ">=3.11"
dependencies = [
    "openai>=1.50.0",
    "azure-identity>=1.19.0",
]

[tool.uv]
package = false
